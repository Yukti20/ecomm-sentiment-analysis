In this project, I'm fine-tuning a sentiment anlaysis model from hugging face and using it for classification of Amazon Product Reviews.

Create a virtual environment in your machine and install the required libraries using requirements.txt file. Create a kernel if required (if jupyter notebook can access the virtual env directly, then it is not required). 
